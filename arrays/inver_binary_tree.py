# Definition for a binary tree node.
class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right


class Solution:
    def invertTree(self, root: TreeNode) -> TreeNode:
        return root

def create_tree_from_vec(root_node = None, value_list = []):
    pass

if __name__ == "__main__":
    
    for val in tree_list:
        TreeNode

    tree = TreeNode([1,5,3,6,7,9,5])
    print(tree)
    print(tree.val)
    print(tree.left)
    print(tree.right)